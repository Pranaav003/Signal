{
  "name": "signal",
  "private": true,
  "description": "Run `npm run dev` from repo root — runs DB migrations, then API + worker + Vite.",
  "scripts": {
    "dev": "bash scripts/dev-all.sh"
  }
}
